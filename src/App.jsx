import { Box } from "@chakra-ui/react";
import Layout from "./Layout";

function App() {
    return (
        <Box height="calc(100vh)">
            <Layout />
        </Box>
    );
}

export default App;
